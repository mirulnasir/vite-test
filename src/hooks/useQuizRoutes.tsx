import * as React from "react";
import { routes } from "../config/routeConfig";

interface IuseQuizRoutes {}

export const routeToArr = routes.map((route, i) => {});

const useQuizRoutes = ({}: IuseQuizRoutes) => {
  return <></>;
};

export default useQuizRoutes;
